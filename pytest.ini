[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
norecursedirs = data checkpoints venv venv_rocm .git
markers =
    gpu: marks tests requiring GPU acceleration
    slow: marks slow running tests
