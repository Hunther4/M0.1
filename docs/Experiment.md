# 🧪 Experiment Manager & Traceability

- Automatic run folders (`runs/0001/`, `runs/0002/`).
- Artifacts: `environment.txt` (Git hash/diff, hardware, versions), `config.json`, `metrics.jsonl`, `metrics.csv`, `training_profile.json`, `summary.json`.
