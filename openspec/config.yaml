# openspec/config.yaml
schema: spec-driven

context: |
  Project: M0.1 — Educational autoregressive decoder-only transformer
  Tech stack: Python 3.10+ / 3.11 (PyTorch, no HuggingFace Trainer)
  Architecture: Modular Python package (src/tokenizer, src/dataset, src/tools)
  Testing: pytest 9.1.1, 46 tests (all passing)
  Style: PEP 8, no formatter/linter installed yet
  Fase 0 complete: BPE tokenizer, dataset prep, token counter CLI
  Next: Fase 1 — Model Architecture

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: true
    test_command: "pytest -v"
  verify:
    test_command: "pytest -v"
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
